;data para los filtros

auxBuffer0Size db 0ffh ;ff es un estado no valido
auxBuffer0 db 07fh dup(0)
;auxBuffer1Size db 0ffh ;no necesitamos auxBuffer1 porque por ahora suponemos que ambos tienen el mismo tamanno
auxBuffer1 db 07fh dup(0)


